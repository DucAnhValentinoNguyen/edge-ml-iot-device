[project]
name = "edge-ml-iot-device"
version = "0.1.0"
description = "A production-shaped demo of deploying a compact ML model into an IoT device."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"

